// function logURL(requestDetails) {
//     if (requestDetails.url.includes('api')) {
//     	console.log(`Loading: ${requestDetails.url}`, requestDetails);
//     }
// }
// chrome.webRequest.onBeforeRequest.addListener(logURL, { urls: ["<all_urls>"] });
